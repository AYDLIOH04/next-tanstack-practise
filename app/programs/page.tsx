import { ProgramList } from "./(components)";

const Programs = () => {
  return (
    <main className="min-h-[calc(100vh-64px)] px-20 flex items-center justify-center text-start">
      <ProgramList />
    </main>
  );
};

export default Programs;
